// Set default container for all workflows
process.container = 'unbeqonet:local'

profiles {
  docker {
    docker.enabled = true
    docker.runOptions = "--user=root"
    docker.fixOwnership = true
  }
}